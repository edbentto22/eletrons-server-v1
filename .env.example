# YOLO Training System - Exemplo de Variáveis de Ambiente (PRD-compliant)
# Copie este arquivo para .env e ajuste os valores conforme necessário

# -----------------------------------------------------------------------------------
# Servidor
# -----------------------------------------------------------------------------------
HOST=0.0.0.0
PORT=8000
DEBUG=false
LOG_LEVEL=info
WORKERS=1
RELOAD=false
ACCESS_LOG=true

# -----------------------------------------------------------------------------------
# Segurança
# -----------------------------------------------------------------------------------
# Token de API para autenticação Bearer em todos os endpoints protegidos
# Deve ser mantido em segredo e compartilhado apenas com clientes autorizados
API_SECRET=changeme_super_secret_token

# Segredo para validação de callbacks externos (se aplicável)
CALLBACK_SECRET=changeme_callback_secret

# Lista de origens permitidas (CORS). Formato JSON array.
# Ex.: ["http://localhost:3000", "https://seu-dominio.com"]
ALLOWED_ORIGINS=["http://localhost:3000"]

# -----------------------------------------------------------------------------------
# Diretórios de dados e logs (persistência)
# -----------------------------------------------------------------------------------
# Diretório base para dados do sistema
DATA_DIR=./data
# Diretório para datasets
DATASETS_DIR=./data/datasets
# Diretório para modelos
MODELS_DIR=./data/models
# Diretório para checkpoints
CHECKPOINTS_DIR=./data/checkpoints
# Diretório para jobs de treino
JOBS_DIR=./data/jobs
# Diretório de logs
LOGS_DIR=./logs

# -----------------------------------------------------------------------------------
# YOLO / Ultralytics
# -----------------------------------------------------------------------------------
# Modelo padrão (inclua extensão .pt, conforme PRD)
DEFAULT_MODEL=yolov8n.pt
# Dispositivo padrão: cpu ou cuda
DEFAULT_DEVICE=cpu
# Hiperparâmetros padrão
EPOCHS=10
BATCH_SIZE=16
IMAGE_SIZE=640
LEARNING_RATE=0.001

# Modelos suportados (separados por vírgula)
SUPPORTED_MODELS=yolov8n.pt,yolov8s.pt,yolov8m.pt,yolov8l.pt,yolov8x.pt

# -----------------------------------------------------------------------------------
# Parâmetros de Treino
# -----------------------------------------------------------------------------------
MAX_CONCURRENT_JOBS=1
JOB_TIMEOUT_SECONDS=86400
SAVE_CHECKPOINTS=true

# -----------------------------------------------------------------------------------
# Monitoramento do Sistema
# -----------------------------------------------------------------------------------
ENABLE_SYSTEM_MONITOR=true
MONITOR_INTERVAL_SECONDS=5
GPU_CHECK_INTERVAL_SECONDS=30

# -----------------------------------------------------------------------------------
# Configuração de Logging
# -----------------------------------------------------------------------------------
# Níveis: critical, error, warning, info, debug
LOGGING_LEVEL=info
LOGGING_JSON=false

# -----------------------------------------------------------------------------------
# Performance / Desenvolvimento
# -----------------------------------------------------------------------------------
# Ajuste para tuning fino; mantenha defaults se não tiver necessidade específica
NUM_WORKERS=1
USE_UVLOOP=false

# -----------------------------------------------------------------------------------
# Observações
# -----------------------------------------------------------------------------------
# - As variáveis acima estão alinhadas ao PRD e ao app/core/config.py
# - ALLOWED_ORIGINS substitui o antigo CORS_ORIGINS
# - API_SECRET é obrigatório para acessar endpoints protegidos
# - CALLBACK_SECRET é opcional e usado para validação de callbacks
# - Ajuste DATA_DIR e subdiretórios conforme persistência desejada